<div class="wrapper kode-header-class-3">
    <header class="header-3">
        <div class="container">
            <div class="logo-container">
                <div class="row">
                    <div class="col-md-2">
                        <!--LOGO START-->
                        <div class="logo">
                            <a href="{{ path('front_default_index') }}"><img src="{{ asset("assets/front/images/logo-1.png") }}" alt=""></a>
                        </div>
                        <!--LOGO END-->
                    </div>
                    <div class="col-md-10">
                            <div class="kode-navigation">
                                <ul>
                                    <li><a href="{{ path('front_default_index') }}">Home</a></li>

                                    <li><a href="{{ path('front_default_index') }}">About Us</a></li>
                                    <li><a href="{{ path('search_results') }}">Our Books</a></li>
                                    <li><a href="{{ path('front_default_index') }}">Blog</a></li>
                                    {% if is_granted('ROLE_USER') %}
                                        <li><a href="#">{{ app.user.username }}</a>
                                            <ul>
                                                {% if is_granted('ROLE_ADMIN') %}
                                                    <li><a href="{{ path('book_index') }}">Manage Books</a></li>
                                                    <li><a href="{{ path('theme_index') }}">Manage Themes</a></li>
                                                    <li><a href="{{ path('category_index') }}">Manage Categories</a></li>
                                                {% endif %}
                                                <li><a href="{{ path('fos_user_profile_show') }}">Show profile</a></li>
                                                <li><a href="{{ path('fos_user_security_logout') }}">Logout</a></li>
                                            </ul>
                                        </li>
                                    {% else %}
                                        <li><a href="{{ path('fos_user_security_login') }}">Login</a>
                                            <ul>
                                                <li><a href="{{ path('fos_user_security_login') }}">Login</a></li>
                                                <li><a href="{{ path('fos_user_registration_register') }}">Register</a></li>
                                            </ul>
                                        </li>
                                    {% endif %}
                                    <li class="search-form">
                                        <form action="" class="form">
                                            <input class="searchBar" placeholder="Search..."/>
                                        </form>
                                    </li>
                                </ul>
                            </div>
                        <div id="kode-responsive-navigation" class="dl-menuwrapper">
                            <button class="dl-trigger">Open Menu</button>
                            <ul class="dl-menu">
                                <li class="menu-item kode-parent-menu"><a href="{{ path('front_default_index') }}">Home</a>
                                    <ul class="dl-submenu">
                                        <li><a href="{{ path('front_default_index') }}">Home page 1</a></li>
                                    </ul>
                                </li>
                                <li><a href="{{ path('front_default_index') }}">About Us</a></li>
                                <li class="menu-item kode-parent-menu"><a href="{{ path('search_results') }}">Our Books</a></li>
                                <li class="menu-item kode-parent-menu"><a href="{{ path('front_default_index') }}">Blog</a></li>
                                {% if is_granted('ROLE_USER') %}
                                    <li class="menu-item kode-parent-menu"><a href="#">{{ app.user.username }}</a>
                                        <ul class="dl-submenu">
                                            {% if is_granted('ROLE_ADMIN') %}
                                                <li><a href="{{ path('book_index') }}">Manage Books</a></li>
                                                <li><a href="{{ path('theme_index') }}">Manage Themes</a></li>
                                                <li><a href="{{ path('category_index') }}">Manage Categories</a></li>
                                            {% endif %}
                                            <li><a href="{{ path('fos_user_profile_show') }}">Show profile</a></li>
                                            <li><a href="{{ path('fos_user_security_logout') }}">Logout</a></li>
                                        </ul>
                                    </li>
                                {% else %}
                                    <li><a href="{{ path('fos_user_security_login') }}">Login</a>
                                        <ul>
                                            <li><a href="{{ path('fos_user_security_login') }}">Login</a></li>
                                            <li><a href="{{ path('fos_user_registration_register') }}">Register</a></li>
                                        </ul>
                                    </li>
                                {% endif %}
                                <li>
                                    <form action="" class="form">
                                        <input class="searchBar" placeholder="Search..."/>
                                    </form>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </header>
</div>