{% extends 'FrontBundle::base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-lg-offset-3 col-md-offset-3 col-sm-offset-1 col-lg-6 col-md-6 col-sm-10">
            <div class="new_edit">
                {{ form_start(form) }}
                <div class="form-group">
                    <h3>Edit Theme</h3>
                </div>
                <div class="form-group">
                        {{ form_widget(form.name, {'attr':{'placeholder':'Title', 'aria-describedby':'basic-addon1', 'class':'form-control'}}) }}
                </div>
                {{ form_rest(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}